 function Notify() {
    let type        = "transaction";
    let from        = null; // 发送者
    let to          = null; // 接收者
    let hash        = null; // txid
    let amount      = 0;    // 转账金额
    let blockNumber = 0;    // 区块高度
};

module.exports = Notify;
